**** PROPERTIES SKAFFOLDER ****
{
    "forEachObj": "oneTime",
    "_partials": []
}
**** END PROPERTIES SKAFFOLDER ****
{
  "name": "{{toFileName project.name}}",
  "version": "1.0.0",
  "private": true,
  "description": "{{toFileName project.name}} generated with Skaffolder Angular 6 - Node JS template",
  "scripts": {
    "install": "cd client && yarn install && cd ../server && yarn install",
    "start": "concurrently --raw \"cd server && yarn start\" \"cd client && yarn run build --watch\" ",
    "build": "concurrently --raw \"cd server && yarn run build\" \"cd client && yarn run build\" ",
    "debug": "concurrently --raw \"cd server && yarn run debug\" \"cd client && yarn run build\" ",
    "lint": "tslint ./**/*.ts -t verbose"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^3.2.0",
    "copy-webpack-plugin": "^4.0.1"
  },
  "repository": {}
}
